#include<gtk/gtk.h>
int main(int argc, char*argv[])
{
	GtkWidget *window;
	gtk_init(&argc,&argv);
	window=gtk_window_new(GTK_WINDOW_TOPLEVEL);
	gtk_container_set_bordwe_width(GTK_CONTAINER(window),300); //taking conta
	gtk_container_show(window); //showing window
	gtk_main(); //monitoring pointer Eg:clicks ,directions or posistions
	return 0;
}
